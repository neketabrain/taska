import styled from "styled-components";

import { Box } from "../Box";

const PageWrapper = styled(Box)`
  max-width: 1152px;
  width: 100%;
  height: 100%;
  margin: 0 auto;
`;

export default PageWrapper;
