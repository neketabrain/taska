import styled from "styled-components";

import { Box } from "../Box";

const PageWrapper = styled(Box)`
  height: 100%;
  margin: 0 auto;
  max-width: 1152px;
  width: 100%;
`;

export default PageWrapper;
